qt_add_executable(config_tests ConfigStoreTest.cpp)
target_link_libraries(config_tests PRIVATE alure_core Qt6::Test)
add_test(NAME ConfigStore COMMAND config_tests)
qt_add_executable(panel_tests PanelHostTest.cpp)
target_link_libraries(panel_tests PRIVATE alure_platform Qt6::Test)
add_test(NAME PanelHostContract COMMAND panel_tests)
qt_add_executable(cli_tests CliTest.cpp)
target_link_libraries(cli_tests PRIVATE Qt6::Test)
target_compile_definitions(cli_tests PRIVATE ALURE_EXECUTABLE="$<TARGET_FILE:alure>")
add_dependencies(cli_tests alure)
add_test(NAME CliSmoke COMMAND cli_tests)
set_tests_properties(ConfigStore PanelHostContract CliSmoke PROPERTIES TIMEOUT 30)
qt_add_executable(service_fixture ServiceFixture.cpp)
target_link_libraries(service_fixture PRIVATE Qt6::Core)
qt_add_executable(services_tests ServicesTest.cpp TrayMenuFixture.h)
target_link_libraries(services_tests PRIVATE alure_services Qt6::Test)
qt_add_executable(tray_menu_demo TrayMenuDemo.cpp TrayMenuFixture.h)
target_link_libraries(tray_menu_demo PRIVATE alure_services)
target_compile_definitions(services_tests PRIVATE SERVICE_FIXTURE="$<TARGET_FILE:service_fixture>")
add_dependencies(services_tests service_fixture)
find_program(DBUS_RUN_SESSION dbus-run-session REQUIRED)
add_test(NAME Services COMMAND ${DBUS_RUN_SESSION} -- $<TARGET_FILE:services_tests>)
set_tests_properties(Services PROPERTIES TIMEOUT 40)
qt_add_executable(ui_tests UiTest.cpp)
target_link_libraries(ui_tests PRIVATE alure_platform alure_core Qt6::Test Qt6::QuickControls2 Qt6::QuickDialogs2)
list(TRANSFORM ALURE_UI_FILES PREPEND "${PROJECT_SOURCE_DIR}/")
qt_add_resources(ui_tests test_ui PREFIX / BASE ${PROJECT_SOURCE_DIR} FILES ${ALURE_UI_FILES})
add_test(NAME QuickUi COMMAND ui_tests)
set_tests_properties(QuickUi PROPERTIES TIMEOUT 30)

qt_add_executable(config_editing_tests ConfigEditingTest.cpp)
target_link_libraries(config_editing_tests PRIVATE alure_core Qt6::Test)
add_test(NAME ConfigEditing COMMAND config_editing_tests)
set_tests_properties(ConfigEditing PROPERTIES TIMEOUT 30)
# Optional private-display process-exit regression, using only installed system tools.
find_program(XVFB_EXECUTABLE Xvfb)
find_program(XDOTOOL_EXECUTABLE xdotool)
if(XVFB_EXECUTABLE AND XDOTOOL_EXECUTABLE)
  add_test(NAME SettingsCloseE2e COMMAND bash ${CMAKE_CURRENT_SOURCE_DIR}/settings-close-e2e.sh $<TARGET_FILE:alure>)
  set_tests_properties(SettingsCloseE2e PROPERTIES TIMEOUT 30)
endif()
