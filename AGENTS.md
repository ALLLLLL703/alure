# Alure 项目协作规则

## 项目定位

- 项目名称：Alure。
- 构建可配置的 Wayland 桌面 Shell，而不是 Wayland 合成器。
- GUI 使用 Qt 6 Quick / QML，核心逻辑和系统集成使用 C++。
- 优先支持和验证 Niri；将合成器相关逻辑隔离，方便后续扩展。
- QML 负责界面、布局、主题与动画；C++ 负责系统服务、数据模型和协议集成。

## 阶段与 Git 提交

- 开始工作前检查 Git 状态，保留用户已有改动。
- 将工作划分为可验证的小阶段，例如工程骨架、面板原型、系统服务接入。
- 每完成一个阶段，执行适用的构建、测试或界面验证，检查 diff，然后立即创建 Git 提交，不要把多个已完成阶段积压到最后提交。
- 提交说明应清楚表达本阶段的变化；仅暂存与当前阶段相关的文件。
- 不提交密钥、构建产物、临时日志或无关改动。
- 验证失败时优先修复；若受环境阻塞，明确记录未验证项，不得宣称阶段验证通过。
- 不擅自重写历史、强制推送或撤销用户改动。提交不意味着自动推送。

## Modern C++

- 以 C++20 为基础，优先使用现代、类型安全、易维护的语言和标准库能力。
- 可在工具链与系统依赖支持、且确有收益时采用 C++23 特性，不为新语法增加不必要的兼容成本。
- 优先使用值语义、RAII、标准容器、明确的所有权和 const 正确性。
- 适当使用 std::optional、std::variant、std::span、std::string_view、ranges 等；注意非拥有视图的生命周期。
- 避免 C 风格强制转换、裸数组、手工资源释放和不必要的宏。

## 内存与资源管理

- 尽可能使用自动存储期对象、容器和 RAII，避免手工 new/delete、malloc/free。
- 独占动态所有权优先使用 std::unique_ptr；仅在确有共享所有权时使用 std::shared_ptr，并避免引用环。
- 裸指针与引用原则上只表示借用，明确其生命周期。
- 尊重 Qt 的 QObject 父子所有权：由父对象管理的对象不再交给另一套拥有型智能指针管理，避免重复释放。
- 对可能提前销毁的 QObject 非拥有引用，按需使用 QPointer；明确 C++ 与 QML 之间的对象所有权。
- 文件描述符、Wayland 对象、订阅、连接和其他系统资源同样采用 RAII 或相应的可靠生命周期管理。

## 依赖与构建

- 优先使用系统包管理器提供的库，通过 CMake find_package、导入目标或 pkg-config 集成。
- 不默认使用 FetchContent、ExternalProject、自动下载或 vendoring 替代系统依赖。
- 系统库缺失或版本不足时，先说明所需包与版本；引入源码下载回退前征求用户同意。
- 保持依赖精简，不为了一个小功能引入大型框架。
- 记录构建依赖和可复现的构建步骤。

## 研究与灵感

- 经常性使用 web search、GitHub CLI 的 gh search 或 GitHub MCP，寻找界面灵感、成熟实现和协议集成经验。
- 新阶段设计、重要技术选型、陌生 API 和疑难问题处理前，主动检索相关资料；不需要为机械性修改重复搜索。
- 优先查阅官方文档、上游源码和维护活跃的项目，核实资料适用的版本。
- 记录影响实现的关键来源；借鉴代码或资源前检查许可证，不盲目复制。
- 不将搜索摘要或 README 宣称当作已验证的运行事实。

## 界面验证与调试

- 经常使用 computer-use MCP 验证实际界面与交互，尤其在完成 GUI 阶段之后。
- 在 Niri 下关注 layer-shell 层级、贴边、exclusive zone、焦点、弹出界面、多屏和缩放。
- 按需使用 DAP MCP 配合 CodeLLDB 调试 C++ 运行时问题，通过断点、调用栈、变量和单步定位根因。
- 调试构建保留调试符号；调用调试工具前检查当前可用工具与会话，不臆造工具能力。
- 若 MCP、图形会话或调试适配器不可用，明确说明限制，并进行可执行的构建或自动化检查；不得声称完成了实际 UI 验证或调试。
- 不擅自修改用户的 Niri 配置、结束现有桌面组件或替换正在运行的 Shell。

## 完成标准

- 每阶段交付明确说明：实现内容、验证方式、已知限制和 Git 提交。
- 模拟数据和占位功能必须明确标记，不冒充真实系统集成。
- 可配置性、常驻资源消耗和可维护性优先于不必要的视觉复杂度。
